# CloudTune Frontend - Приложение на Flutter

## 🎵 О приложении

CloudTune Frontend - это мобильное приложение, разработанное с использованием Flutter. Приложение обеспечивает бесшовный опыт прослушивания музыки на различных устройствах, подключаясь к бэкенду CloudTune для управления учетными записями пользователей, плейлистами и музыкальными коллекциями. Приложение поддерживает воспроизведение локальных аудиофайлов с возможностью фонового воспроизведения.

## 🛠 Используемые технологии

- **Flutter** - кроссплатформенный фреймворк для мобильной разработки
- **Dart** - язык программирования для Flutter
- **Provider** - решение для управления состоянием
- **Shared Preferences** - локальное хранение данных
- **Dio** - HTTP-клиент для связи с API
- **Just Audio** - возможности воспроизведения аудио
- **Audio Service** - фоновое воспроизведение аудио

## 📁 Архитектура приложения

```
frontend/cloudtune_flutter_app/
├── lib/                    # Основной исходный код приложения
│   ├── main.dart         # Точка входа в приложение
│   ├── models/           # Модели данных
│   │   ├── user.dart     # Модель пользователя
│   │   ├── track.dart    # Модель аудиотрека
│   │   └── playlist.dart # Модель плейлиста
│   ├── services/         # Сервисные классы
│   │   ├── api_service.dart    # Коммуникация с API
│   │   ├── auth_service.dart   # Сервис аутентификации
│   │   ├── audio_service.dart  # Сервис воспроизведения аудио
│   │   └── storage_service.dart # Сервис локального хранения
│   ├── screens/          # Экраны UI
│   │   ├── home_screen.dart      # Главная панель
│   │   ├── login_screen.dart     # Экран входа
│   │   ├── register_screen.dart  # Экран регистрации
│   │   ├── profile_screen.dart   # Экран профиля
│   │   ├── local_tracks_screen.dart # Локальные треки
│   │   └── cloud_tracks_screen.dart # Облачные треки
│   ├── widgets/          # Повторно используемые компоненты UI
│   │   ├── audio_player_widget.dart # Элементы управления аудиоплеером
│   │   ├── custom_button.dart      # Пользовательская кнопка
│   │   ├── custom_input.dart       # Пользовательское поле ввода
│   │   └── theme.dart              # Определения темы
│   ├── providers/        # Провайдеры состояния
│   │   ├── auth_provider.dart      # Состояние аутентификации
│   │   ├── audio_provider.dart     # Состояние воспроизведения аудио
│   │   └── theme_provider.dart     # Состояние темы
│   └── utils/            # Вспомогательные функции
│       ├── constants.dart  # Константы и конфигурация
│       ├── validators.dart # Валидация ввода
│       └── helpers.dart    # Вспомогательные функции
├── assets/               # Статические ресурсы (изображения, иконки, аудио)
├── test/                 # Модульные и виджет-тесты
├── pubspec.yaml          # Зависимости проекта и ресурсы
└── README.md             # Этот файл
```

## 🎨 Пользовательский интерфейс

Приложение имеет современный, интуитивно понятный интерфейс с нижней навигацией:

### Основные экраны:
- **Главный экран**: Главная панель с элементами управления воспроизведением аудио и списком треков
- **Экран профиля**: Управление аутентификацией пользователя и профилем
- **Экран локальных треков**: Управление локальными аудиофайлами
- **Экран облачных треков**: Интеграция с облачным хранилищем

### Поток аутентификации:
- **Экран входа**: Форма входа пользователя
- **Экран регистрации**: Форма регистрации пользователя
- **Защищенные маршруты**: Экраны, требующие аутентификации

### Компоненты UI:
- Тематические виджеты с поддержкой светлой/тёмной темы
- Пользовательские кнопки и поля ввода
- Элементы управления воспроизведением с индикаторами прогресса
- Нижняя навигация с поддержкой иконок

## 🌐 Связь с бэкендом

Фронтенд будет взаимодействовать с API бэкенда CloudTune через модуль `services/api_service.dart`:

### API-эндпоинты:
- `POST /auth/register` - Регистрация пользователя
- `POST /auth/login` - Аутентификация пользователя
- `GET /api/profile` - Получение профиля пользователя (защищено)
- `GET /health` - Проверка работоспособности сервера

### Поток коммуникации:
1. **Конфигурация базового URL**: API-запросы будут отправляться на `http://10.0.2.2:8080` (настраивается в `utils/constants.dart`)
2. **Заголовки аутентификации**: JWT-токены будут извлекаться из Shared Preferences и добавляться к запросам
3. **Обработка запросов**: Все API-запросы будут использовать Dio с надлежащей обработкой ошибок
4. **Обработка ответов**: JSON-ответы будут валидироваться и обрабатываться

### Система аутентификации:
- **Хранение токенов**: JWT-токены будут храниться в Shared Preferences с использованием ключа `cloudtune_token`
- **Извлечение токенов**: Токены будут извлекаться перед каждым аутентифицированным запросом
- **Защищенные маршруты**: API-запросы, требующие аутентификации, будут включать заголовок Authorization
- **Управление сессией**: AuthProvider будет управлять состоянием входа/выхода

### Функции сервиса API:
- `checkHealth()` - Проверка подключения к бэкенду
- `registerUser()` - Создание новой учетной записи пользователя
- `loginUser()` - Аутентификация учетных данных пользователя
- `getUserProfile()` - Получение данных аутентифицированного пользователя

## 🎧 Система воспроизведения аудио

Приложение реализует комплексную систему воспроизведения аудио:

### Архитектура сервиса аудио:
- **AudioProvider**: Провайдер состояния для аудиофункциональности
- **AudioService**: Сервис фонового аудио с использованием пакета audio_service
- **Интеграция Just Audio**: Использует библиотеку just_audio для воспроизведения

### Возможности аудио:
- **Управление очередью треков**: Добавление, воспроизведение, пропуск треков
- **Элементы управления воспроизведением**: Функции воспроизведения, паузы, остановки, перемотки
- **Управление громкостью**: Регулируемые уровни громкости
- **Режимы повтора**: Один трек, очередь или выключено
- **Фоновое воспроизведение**: Аудио продолжает воспроизводиться при минимизации приложения
- **Медиа-уведомления**: Системные уведомления с элементами управления воспроизведением
- **Отслеживание прогресса**: Отображение текущей позиции и общей длительности

### Обработка аудиофайлов:
- **Локальное хранилище**: Аудиофайлы хранятся в локальном хранилище устройства
- **Поддержка форматов файлов**: MP3, WAV, M4A, FLAC и другие распространенные форматы
- **Извлечение метаданных**: Названия треков, исполнители и длительность
- **Управление размером файлов**: Эффективное хранение и извлечение

## 🔐 Поток аутентификации

Приложение реализует безопасный поток аутентификации:

1. **Регистрация/вход пользователя** на экране профиля
2. **Хранение JWT-токенов** в Shared Preferences
3. **Автоматическое управление состоянием аутентификации** через AuthProvider
4. **Защищенные API-запросы** с заголовками Authorization
5. **Функциональность выхода** с удалением токена

### Контекст аутентификации:
- **AuthProvider** будет управлять состоянием аутентификации
- **Auth Service** будет обрабатывать коммуникацию с API
- **Сохранение токенов** между сессиями приложения
- **Кэширование данных пользователя** для быстрого доступа

## 🚀 Начало работы

### Предварительные требования

- Flutter SDK (последняя стабильная версия)
- Dart SDK (входит в состав Flutter)
- Android Studio или VS Code с плагином Flutter
- Android SDK (для разработки под Android)
- Xcode (для разработки под iOS, только macOS)

### Инструкции по настройке

1. Клонируйте репозиторий:
   ```bash
   git clone <ссылка-на-репозиторий>
   cd frontend/cloudtune_flutter_app
   ```

2. Установите зависимости:
   ```bash
   flutter pub get
   ```

3. Запустите приложение:
   ```bash
   flutter run
   ```

### Команды разработки

- `flutter run` - запуск приложения на подключенном устройстве/симуляторе
- `flutter run -d chrome` - запуск в веб-браузере
- `flutter devices` - список доступных устройств
- `flutter build apk` - сборка APK для Android
- `flutter build ios` - сборка для iOS

## 🧪 Стратегия тестирования

Приложение включает комплексное тестирование:

- Модульные тесты для бизнес-логики
- Виджет-тесты для компонентов UI
- Интеграционные тесты для коммуникации с API
- Автоматизированный CI/CD пайплайн

Для запуска тестов:
```bash
flutter test
```

## 🚢 Стратегия развертывания

Для сборки приложения для продакшена:

1. Создайте продакшн-сборку:
   ```bash
   flutter build apk --release
   # или для iOS
   flutter build ios --release
   ```

2. Для распространения следуйте рекомендациям для конкретной платформы:
   - Google Play Store для Android
   - Apple App Store для iOS

## 🌟 Возможности приложения

- Адаптивный UI, работающий на различных размерах экрана
- Поддержка светлой и тёмной темы
- Безопасная аутентификация с JWT-токенами
- Кроссплатформенная совместимость (iOS/Android/Web)
- Офлайн-хранилище токенов
- Выбор и воспроизведение локальных аудиофайлов
- Поддержка фонового воспроизведения аудио
- Чистый, современный дизайн UI
- Правильная обработка ошибок и обратная связь с пользователем
- Офлайн-режим для кэшированного контента
- Синхронизированное воспроизведение между устройствами
- Управление плейлистами
- Функция поиска
- Эквалайзер
- Таймер сна
- Отображение текстов песен
- Возможность социального обмена

## 📄 Лицензия

Этот проект лицензирован под лицензией MIT - см. файл [LICENSE](../../LICENSE) для получения подробной информации.